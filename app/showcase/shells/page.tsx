/**
 * Shells tab — a live gallery of every nav shell the design agent can pick
 * from. See showcase/shells/ShellCatalog.tsx.
 */
import { ShellCatalog } from "@/showcase/shells/ShellCatalog";

export default function ShellsPage() {
  return <ShellCatalog />;
}
